import { Link } from "expo-router";
import { Text, View  } from "react-native";

export default function Index() {
  return (
    <View>
      <Text style={{ fontSize: 20, fontWeight: "bold", color: "red", textAlign: "center", fontStyle: "bold" }}>
        Welcome to the Mini-Project
      </Text>
    </View>
  );
}
